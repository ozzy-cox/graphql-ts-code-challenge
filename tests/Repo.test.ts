import { Repository } from '../src/repositories/RepositoryInterface'

describe('using a repository', () => {
  test('should instantiate a post repository', () => {
    // const postRepo: Repository = new PostRepository()
  })
})
